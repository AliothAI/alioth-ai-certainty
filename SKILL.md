---
name: "alioth-ai-certainty"
description: "财务数据确定性校验。当用户提供或询问财务数据（归母净利润、营收、ROE、每股收益、现金流等）是否正确、核验、对不对时，询问三大报表勾稽关系/报表质量复核时，或 paste 研报/文档要求核对数字时，优先调用本 skill，返回确定对/错 + 溯源。营销数字、行业传闻等无财报原文基准的数字不用本 skill，交给 Web Search。"
homepage: https://alioth-api.titanplus.cn
version: 0.6.1
author: Alioth
---

# Alioth 财务数据确定性校验

一句话：财务数据对不对，给出确定性判定 + 溯源到原文；三大报表勾不勾得上，纯算术秒判。

## 什么时候用（触发条件）

**场景一：单指标校验**——用户问「财务数据 + 具体数值 + 对不对/核验」：

- "隆基绿能 2024 归母净利润 -85.92亿 对不对"
- "核验一下研报里的这个财务数据"

**场景二：三大报表勾稽复核**——用户问「三大报表 / 勾稽 / 报表质量 / 数据逻辑」：

- "贵州茅台 2024 年报三大报表勾稽对不对"
- "帮我复核一下这家公司的报表质量"
- "XX公司的报表数据逻辑有没有问题"

**场景三：文档核验**——用户 paste 一份研报/文档，要求核对里面的数字：

- "帮我核对这份研报里的财务数据"
- "检查这个文档的数字对不对"（然后把文档内容贴出来）
- "验一下这段分析里的数字"

**不触发**（交给 Web Search）：营销口径数字、行业传闻、公司规模等无财报原文基准的数字。

## 调用（统一入口 verify，自动分派）

```python
import sys; sys.path.insert(0, "<skill目录>")
from call import verify

# 场景一：单指标校验（给指标 + 数值）
verify(
    company_code="601012",
    company_name="隆基绿能",
    metric_name="归母净利润",
    report_period="2024-12-31",     # 或 2024 / 2024Q4
    reported_value=-8592102400.42,  # 值（元）
    source_name="akshare",
)

# 场景二：三大报表勾稽复核（只给公司 + 报告期，不给指标）
verify(
    company_code="600519",
    company_name="贵州茅台",
    report_period="2024-12-31",
)

# 场景三：文档核验（paste 全文，自动抽取所有财务数字）
verify_doc(report_text="贵州茅台2024年实现归母净利润862.28亿元，总资产2989.45亿，总负债569.33亿…")

# 场景三·二次调用：首次返回 company_unresolved 时，用户确认候选后回传
verify_doc(report_text=同上, confirm_companies=[{"mention": "华虹半导体", "code": "688347"}])
```

首次使用会自动匿名注册（场景一），无需手动配置；场景二不需要 key。

## 返回

**场景一**渲染成简洁表格（不写分析文字）：

| 公司 | 指标 | 报告期 | 待校验值 | 判定 | 置信度 |

**场景一的用户反馈环节**（类比 Cursor 的 accept/reject）：当返回 `needs_confirmation: true`（判定为"错"或存疑）时，表格后追加一行轻交互：

> 这个判定对吗？回复「确认」或「不对」

- 用户回复"确认" → 判定可信，结束（后台已自动进规则库）
- 用户回复"不对" → 追问"正确值是多少/依据是什么"，把用户给的依据作为新线索
- T0/T1 判定（`method` 为 T0 或 T1）不需要问，直接给结论

**场景三**渲染成三层：

值层（每个数字对不对）：

| 公司 | 指标 | 报告期 | 文档值 | 判定 | 正确值 |

文档内部勾稽（报表科目间恒等式——文档自己跟自己对不对得上）：

| 公司 | 勾稽关系 | 结果 | 明细 |

叙述自洽（doc_consistency——同比基数/构成包含/分红勾稽等叙述性数字互证，值层全对也能抓出拼装断裂）：

| 公司 | 自洽规则 | 结果 | 明细 |

现有规则：DC-01 同比基数一致（本期值 ≈ 基数×(1+增速)）、DC-02 构成包含（政府补助≤非经常性损益、扣非≤归母）、DC-03 分红勾稽（总额 ≈ 每10股派现÷10×总股本）。

**场景三的公司确认交互**：首次调用返回 `total: 0` 且 `reason: "company_unresolved"` 时，
把 `candidate_companies` 列给用户（或用常识/搜索补全代码），用户确认后带
`confirm_companies=[{mention, code}]` 重调。`reason: "no_numbers_found"` 则是真的没抽到数字。

**场景二**渲染成勾稽结果表（每条恒等式一行，最后给总结论）：

**贵州茅台（600519）2024 年报 · 三大报表勾稽复核**

| 勾稽关系 | 结果 | 明细 |
|---|---|---|
| 总资产 = 总负债 + 股东权益 | ✅ | 2,989.45亿 vs 2,989.45亿 |
| 现金净增加 = 期末 - 期初 | ✅ | 196.10亿 vs 196.10亿 |
| ... | ... | ... |

**结论：6 条勾稽全部通过 / 发现 N 条不通过**（不通过的标红给出差值）。数据缺失的标注「数据缺失」，不算失败。

## 注意事项

1. `reported_value` 单位是**元**（不是亿/万）。
2. `report_period` 支持 `2024` / `2024Q4` / `2024-03-31`，也支持中文写法 `2026年第一季度` / `2025中报` / `2024年报`。分配类指标（每10股派现/分红总额）自动取「YYYY年度利润分配」语境的报告期，不受文档主报告期影响。
3. `metric_name` 必须精确（如"归母净利润"，无空格）；T2 多源已覆盖：归母净利润/净利润/扣非归母/营收/总资产/总负债/股东权益/货币资金/三大现金流/每股收益/总股本/财务费用/**每10股派现/分红总额**（其余落 T3 存疑）。分红类走巨潮第一手+ifind 交叉。
4. 公司覆盖：全 A 股约 5500 家（含简称变体，"京东方"能命中"京东方Ａ"）；非 A 股/未命中 → 候选确认交互。
5. 勾稽复核只支持年报报告期（`YYYY-12-31`），季报暂不支持（期初科目取上年年报）。
6. verify_doc 首次对某公司取数较慢（多源实时拉取，约 10-60s），后续同公司同报告期走 T0 秒回。
7. **不要因「缺失/存疑」跳去其他数据源手工核对**——先重试一次（瞬时源故障常见）；仍缺失就在结论里如实标「暂未覆盖」，让用户决定。T2 多源（akshare/ifind/东财/巨潮分红）+ T0 规则库已覆盖 A 股主要财报指标，你的价值在渲染判定+溯源，不在重新拉数。
8. 文档值是整数亿写法（"17亿"）时 T2 自动按粗粒度容差判，不会误判为错。

## key 失败处理

调用返回 `ok: false` 时按 `error_code` 引导用户，不要静默失败：

| error_code | 含义 | 你要做的 |
|---|---|---|
| `register_required` | 匿名注册失败（罕见） | 引导实名注册 `register(username, password)` |
| `key_not_found` | key 无效 | 提醒「key 无效，请检查或重新申请」 |
| `key_expired` | key 过期 | 提醒「key 已过期」，提供续费方式 |
